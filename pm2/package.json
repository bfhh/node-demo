{
  "name": "pm2",
  "version": "1.0.0",
  "description": "```\r npm install -g pm2\r ```",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env NODE_ENV=dev nodemon app.js",
    "prd": "cross-env NODE_ENV=production pm2 start pm2.config.json"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cross": "^1.0.0",
    "cross-env": "^7.0.3",
    "nodemon": "^2.0.7"
  }
}
